Running The PathFinder
Note: The compiler is tested using Python 3.7.1

First, install all the required packages in your venv:

$ pip install -r requirements.txt

Then, run index.py:

$ python index.py


Follow the "Instructions" and correct format usage for error free path finding